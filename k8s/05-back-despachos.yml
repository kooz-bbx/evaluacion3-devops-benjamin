########################################
# Deployment back-despachos (puerto 8081)
########################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-despachos
  namespace: eval2
  labels:
    app: back-despachos
spec:
  replicas: 2          # Alta disponibilidad: 2 réplicas
  selector:
    matchLabels:
      app: back-despachos
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0   # Zero-downtime deploy
  template:
    metadata:
      labels:
        app: back-despachos
    spec:
      containers:
        - name: back-despachos
          # CORREGIDO: Se añade el ID real de tu cuenta AWS Academy
          image: 420642166761.dkr.ecr.us-east-1.amazonaws.com/eval2-back-despachos:latest
          ports:
            - containerPort: 8081
          env:
            - name: MYSQL_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: eval2-config
                  key: MYSQL_DATABASE
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: eval2-config
                  key: DB_USERNAME
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: eval2-config
                  key: DB_HOST
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: eval2-secret
                  key: DB_PASSWORD
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: eval2-secret
                  key: MYSQL_ROOT_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"

---
########################################
# Service back-despachos (solo interno)
########################################
apiVersion: v1
kind: Service
metadata:
  name: back-despachos
  namespace: eval2
  labels:
    app: back-despachos
spec:
  selector:
    app: back-despachos
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP